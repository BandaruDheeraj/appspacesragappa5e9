name: Trigger auto deployment for container-app-hablr76cfj65y

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-hablr76cfj65y-AutoDeployTrigger-e4c41999-f0c5-4693-bfb6-77a6950c03b4.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPHABLR76CFJ65Y_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPHABLR76CFJ65Y_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPHABLR76CFJ65Y_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPHABLR76CFJ65Y_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPHABLR76CFJ65Y_REGISTRY_PASSWORD }}
          containerAppName: container-app-hablr76cfj65y
          resourceGroup: appspacesragappa5e9b2a8
          imageToBuild: default/container-app-hablr76cfj65y:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


